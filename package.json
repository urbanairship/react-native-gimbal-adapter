{
  "private": true,
  "workspaces": [
    "src",
    "example"
  ],
  "jest": {
    "preset": "react-native"
  },
  "scripts": {
    "start": "react-native start",
    "generate-docs": "documentation --config documentation.yml build src/js/** -f html -o out",
    "test": "jest --verbose",
    "clean": "rm -rf node_modules src/node_modules example/node_modules yarn.lock"
  },
  "devDependencies": {
    "documentation": "13.0.1",
    "typedoc": "^0.17.7"
  }
}
